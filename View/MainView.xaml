<Window x:Class="SimReeferMiddlewareSystemWPF.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimReeferMiddlewareSystemWPF.View.ProtocolVer.Ver8"
        xmlns:viewmodel="clr-namespace:SimReeferMiddlewareSystemWPF.ViewModel.ProtocolVer.Ver8"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        mc:Ignorable="d"
        Title="EyeCargo Reefer Middleware Simulator" Height="800" MinWidth="1850">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodel:ProtocolViewModelVer8}">
            <local:ProtocolVer8/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:DeviceInfoViewModelVer8}">
            <local:DeviceInfoVer8/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:SetupInfoViewModelVer8}">
            <local:SetupInfoVer8/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:DeviceBodyViewModelVer8}">
            <local:DeviceBodyVer8/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:ReeferBodyViewModelVer8}">
            <local:ReeferBodyVer8/>
        </DataTemplate>
        <Style x:Key="MyTextStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style x:Key="MyTextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style x:Key="MyButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style x:Key="MyTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="TextAlignment" Value="Left"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition />
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Height="70" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Height="50" VerticalAlignment="Top" Background="White" >
                <Menu x:Name="menu1" VerticalAlignment="Center">
                    <Menu.BitmapEffect>
                        <DropShadowBitmapEffect/>
                    </Menu.BitmapEffect>
                    <MenuItem Header="Menu" Background="White" VerticalAlignment="Top" Style="{StaticResource MyTextStyle}">
                        <MenuItem Header="Send Manual Packet"/>
                        <Separator/>
                        <MenuItem Header="ID Server"/>
                        <Separator/>
                        <MenuItem Header="FOTA Server"/>
                    </MenuItem>
                </Menu>
            </Grid>
            <Grid Grid.Row="1" VerticalAlignment="Stretch" Background="White" Name="TopGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="210"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="IP:" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource MyTextBlockStyle}"/>
                <TextBox Grid.Column="1" Text="{Binding IP}" Style="{StaticResource MyTextBoxStyle}" BorderThickness="1" BorderBrush="Black"/>
                <TextBlock Grid.Column="2" Text="PORT:" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource MyTextBlockStyle}"/>
                <TextBox Grid.Column="3" Text="{Binding Port}" Style="{StaticResource MyTextBoxStyle}" BorderThickness="1" BorderBrush="Black"/>
                <TextBlock Grid.Column="4" Text="VERSION:" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource MyTextBlockStyle}"/>
                <TextBox Grid.Column="5" Text="{Binding ProtocolVersion}" Style="{StaticResource MyTextBoxStyle}" BorderThickness="1" BorderBrush="Black" Margin="3,0,3,0"/>
                <Button Grid.Column="6" Command="{Binding ToConnectCommand}" IsEnabled="{Binding IsEnabled}" Content="Connect" Style="{StaticResource MyButtonStyle}" Margin="3,0,3,0"/>
                <Button Grid.Column="7" Command="{Binding ToDisconnectCommand}" Content="Disconnect" Style="{StaticResource MyButtonStyle}" Margin="3,0,3,0"/>
                <Image Grid.Column="8" gif:ImageBehavior.AnimatedSource="{Binding ToLoadImage}" Margin="3,0,3,0"/>
            </Grid>
        </Grid>
        <Grid Grid.Row="1">
            <TextBlock Text="Body" FontSize="10"/>
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Grid>
        <Grid Grid.Row="2" Height="150" Background="LightGoldenrodYellow">
            <Border BorderBrush="Black" BorderThickness="1"/>
            <TextBlock Text="Received Message" FontSize="10"/>
        </Grid>
    </Grid>
</Window>
