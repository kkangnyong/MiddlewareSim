<UserControl x:Class="SimReeferMiddlewareSystemWPF.View.ProtocolVer.Ver8.ProtocolVer8"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimReeferMiddlewareSystemWPF.View.ProtocolVer.Ver8"
             xmlns:vm="clr-namespace:SimReeferMiddlewareSystemWPF.ViewModel.ProtocolVer.Ver8"
             mc:Ignorable="d">
    <!--<UserControl.DataContext>
        <vm:ProtocolViewModel/>
    </UserControl.DataContext>-->
    <Grid Margin="0,0,0,0" Background="AliceBlue">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" VerticalAlignment="Stretch">
                <Border BorderBrush="Black" BorderThickness="1"/>
                <TextBlock Text="DeviceInfo" FontSize="30"/>
                <ContentControl Content="{Binding CurrentDeviceInfoViewModel}" Margin="0,0,0,0"/>
            </Grid>
            <Grid Grid.Row="1" VerticalAlignment="Bottom" Height="30">
                <Button Command="{Binding ToDeviceInfoCommand}" Content="DeviceInfo" VerticalAlignment="Bottom" Margin="5"/>
            </Grid>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" VerticalAlignment="Stretch">
                <Border BorderBrush="Black" BorderThickness="1"/>
                <TextBlock Text="SetupInfo" FontSize="30"/>
                <ContentControl Content="{Binding CurrentSetupInfoViewModel}" Margin="0,0,0,0"/>
            </Grid>
            <Grid Grid.Row="1" VerticalAlignment="Bottom" Height="30">
                <Button Command="{Binding ToSetupInfoCommand}" Content="SetupInfo" VerticalAlignment="Bottom" Margin="5"/>
            </Grid>
        </Grid>
        <Grid Grid.Column="2">
            <Border BorderBrush="Black" BorderThickness="1"/>
            <TextBlock Text="Start Data" FontSize="30"/>
            <Button Command="{Binding ToStartDataCommand}" Content="Start Data" VerticalAlignment="Center" Margin="5" Height="147"/>
        </Grid>
        <Grid Grid.Column="3">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" VerticalAlignment="Stretch">
                <Border BorderBrush="Black" BorderThickness="1"/>
                <TextBlock Text="DeviceData" FontSize="30"/>
                <ContentControl Content="{Binding CurrentDeviceBodyViewModel}"/>
            </Grid>
            <Grid Grid.Row="1" VerticalAlignment="Bottom" Height="30">
                <Button Command="{Binding ToDeviceBodyCommand}" Content="Device Data" VerticalAlignment="Bottom" Margin="5" />
            </Grid>
        </Grid>
        <Grid Grid.Column="4">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" VerticalAlignment="Stretch">
                <Border BorderBrush="Black" BorderThickness="1"/>
                <TextBlock Text="ReeferData" FontSize="30"/>
                <ContentControl Content="{Binding CurrentReeferBodyViewModel}"/>
            </Grid>
            <Grid Grid.Row="1" VerticalAlignment="Bottom" Height="30">
                <Button Command="{Binding ToReeferBodyCommand}" Content="Reefer Data" VerticalAlignment="Bottom" Margin="5" />
            </Grid>
        </Grid>
        <Grid Grid.Column="5">
            <Border BorderBrush="Black" BorderThickness="1"/>
            <TextBlock Text="Start Command" FontSize="30"/>
            <Button Command="{Binding ToStartCommandCommand}" Content="Start Command" VerticalAlignment="Center" Margin="5" Height="147"/>
        </Grid>
    </Grid>
</UserControl>
    
